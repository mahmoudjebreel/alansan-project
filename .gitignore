/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
*.sql
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed
/bootstrap/cache/*.php
!/bootstrap/cache/.gitignore

.vercel
.env*

# local diagnostics / scratch
/repro_*.php
/diagnose_*.php

# Files uploaded through the panel. The directories are kept (and shipped) so
# the server never has to create them; what people upload is not committed.
/public/uploads/*
!/public/uploads/.gitkeep
